--select the people row from the movies table
SELECT
     "birth"
--we should say that which table we're actually query to it
FROM
     "people"
--select the emma watsonname and year of birth
WHERE
     "name" LIKE "emma stone"